name: Download Aigei Music (Auto & Xvfb)

on:
  workflow_dispatch:
    inputs:
      pages:
        description: '抓取翻页数量 (Pages, 每页约25首; 填 0 为翻到最后一页)'
        required: false
        default: '5'
        type: string
      delay:
        description: '每首歌下载间隔延迟秒数 (Delay, 建议 2~5 秒防风控)'
        required: false
        default: '3.0'
        type: string
      push_interval:
        description: '每成功下载多少首就自动 Git Commit & Push 提交到仓库'
        required: false
        default: '100'
        type: string
      url:
        description: '抓取的目标页面 URL'
        required: false
        default: 'https://www.aigei.com/music/midi/'
        type: string
      cookie:
        description: '自定义 Cookie (留空则默认读取 Secrets.AIGEI_COOKIE)'
        required: false
        default: ''
        type: string

# 授予工作流向仓库推送代码与文件的权限
permissions:
  contents: write

jobs:
  crawl-aigei:
    name: Crawl & Download Aigei Audio
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 最长运行 6 小时

    steps:
      - name: 1. 检出仓库代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 2. 配置 Python 3.11 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 3. 安装依赖与 Xvfb 虚拟显示器
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 playwright tqdm
          playwright install chromium --with-deps
          # 安装 Xvfb 虚拟显示器（启动 1080P 虚拟桌面，让浏览器以真实桌面窗口运行，彻底消除 Headless 爬虫特征）
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: 4. 配置 Cookie 凭证
        env:
          INPUT_COOKIE: ${{ github.event.inputs.cookie }}
          SECRET_COOKIE: ${{ secrets.AIGEI_COOKIE }}
        run: |
          COOKIE_VAL=""
          if [ -n "$INPUT_COOKIE" ]; then
            COOKIE_VAL="$INPUT_COOKIE"
            echo "✅ 使用手动输入的 Cookie"
          elif [ -n "$SECRET_COOKIE" ]; then
            COOKIE_VAL="$SECRET_COOKIE"
            echo "✅ 使用 Secrets.AIGEI_COOKIE 中的配置"
          fi

          if [ -n "$COOKIE_VAL" ]; then
            echo "$COOKIE_VAL" > downmidi/cookie.txt
            echo "✅ Cookie 已成功写入 downmidi/cookie.txt"
          else
            echo "⚠️ 未输入新 Cookie，将直接使用代码/本地已有的配置"
          fi

      - name: 5. 启动虚拟显示器并执行全自动抓取与分段提交
        env:
          DISPLAY: ':99'
          GITHUB_ACTIONS: 'true'
        run: |
          # 启动 Xvfb 虚拟显示器（1920x1080 分辨率，24位真实色深）
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 3
          echo "🖥️ Xvfb 虚拟屏幕已启动在 $DISPLAY"

          # 启动全自动抓取脚本
          PAGES="${{ github.event.inputs.pages || '5' }}"
          DELAY="${{ github.event.inputs.delay || '3.0' }}"
          PUSH_INTERVAL="${{ github.event.inputs.push_interval || '100' }}"
          TARGET_URL="${{ github.event.inputs.url || 'https://www.aigei.com/music/midi/' }}"

          # 在 Xvfb 下加上 --gui，浏览器即以真实的桌面窗口模式运行在虚拟屏幕上
          python downmidi/auto_downloader.py \
            --pages "$PAGES" \
            --delay "$DELAY" \
            --push-interval "$PUSH_INTERVAL" \
            --url "$TARGET_URL" \
            --gui

      - name: 6. 兜底提交未保存的音频与记录
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add downmidi/downloads/ downmidi/downloaded_ids.txt downmidi/download_success.txt || true
          if ! git diff --staged --quiet; then
            git commit -m "chore: 兜底提交本次爱给网下载成果与历史记录 [skip ci]"
            git pull --rebase || true
            git push || true
            echo "✅ 最终成果已成功推送到仓库"
          else
            echo "ℹ️ 无新增文件需提交"
          fi

      - name: 7. 上传 Artifact 压缩包附件
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aigei-audio-downloads
          path: |
            downmidi/downloads/
            downmidi/downloaded_ids.txt
            downmidi/download_success.txt
          if-no-files-found: ignore
          retention-days: 30
